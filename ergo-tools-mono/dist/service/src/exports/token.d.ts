export declare class Token {
    tokenId: string;
    amount: number;
    decimals: number;
    name: string;
    tokenType: string;
    constructor(tokenId: string, amount: number, decimals: number, name: string, tokenType: string);
}
